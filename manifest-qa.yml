applications:
  - name: event-driven-service-dev
    memory: 1G
    instances: 1
    path: target/*.jar
    buildpacks:
      - java_buildpack
    env:
	  JBP_CONFIG_OPEN_JDK_JRE: '{jre: { version: 17.+ } }'	
      #SPRING_PROFILES_ACTIVE: dev

	  